# テストと使用ガイド

## プラグインのテスト方法

### 1. ビルド手順

#### IntelliJ IDEA 2025.3.1 を使用
1. プロジェクトを IntelliJ IDEA で開く
2. 右側の Gradle ツールウィンドウを開く
3. `Tasks` → `build` → `build` をダブルクリック
4. ビルド完了後、`build/libs/IronBlockElevator-1.1.0.jar` が生成される

#### コマンドラインを使用
```bash
./gradlew build
```

### 2. サーバーへのインストール

1. Minecraft 1.19.4 対応の Paper または Spigot サーバーを用意
2. `build/libs/IronBlockElevator-1.1.0.jar` をサーバーの `plugins` フォルダにコピー
3. サーバーを起動
4. コンソールに "IronBlockElevator has been enabled!" が表示されることを確認

### 3. 動作テスト

#### 基本的なエレベーターの構築

1. **垂直エレベーターの作成**
   ```
   高さ 80: [鉄ブロック] + 空間2ブロック
   高さ 70: [鉄ブロック] + 空間2ブロック
   高さ 60: [鉄ブロック] + 空間2ブロック
   ```

2. **上昇テスト**
   - 最下層の鉄ブロックの上に立つ
   - ジャンプ（スペースキー）を押す
   - 上の鉄ブロックまで自動的に移動することを確認

3. **下降テスト**
   - 最上層の鉄ブロックの上に立つ
   - スニーク（Shiftキー）を押す
   - 下の鉄ブロックまで自動的に移動することを確認

### 4. コマンドテスト

#### 情報表示
```
/ironblockelevator
```
または
```
/ibe
```
プラグインのバージョンと作者情報が表示されます。

#### 設定リロード
```
/ironblockelevator reload
```
または
```
/ibe reload
```
config.yml の変更を再読み込みします。

### 5. 設定のカスタマイズテスト

1. サーバーを停止
2. `plugins/IronBlockElevator/config.yml` を編集
   ```yaml
   enabled: true
   upward-speed: 1.0      # より高速に変更
   downward-speed: 1.0    # より高速に変更
   max-height: 256
   debug: true            # デバッグモードを有効化
   ```
3. サーバーを起動（または `/ibe reload` を実行）
4. 速度の変化を確認

### 6. エッジケーステスト

#### テストケース 1: 空間が不足している場合
- 鉄ブロックの上に1ブロックしか空間がない場所を作成
- エレベーターがその場所にジャンプしないことを確認

#### テストケース 2: 複数プレイヤー
- 複数のプレイヤーが同時にエレベーターを使用
- それぞれが独立して動作することを確認

#### テストケース 3: 最大高度
- 設定の `max-height` を超える高さに鉄ブロックを配置
- その高さまでジャンプできないことを確認

### 7. パフォーマンステスト

- 多数の鉄ブロックを設置した環境でテスト
- 複数プレイヤーが同時に使用してもラグが発生しないことを確認
- サーバーコンソールでエラーが出ていないことを確認

## トラブルシューティング

### プラグインが読み込まれない
- Java 17以上がインストールされているか確認
- Paper/Spigot 1.19.4 サーバーを使用しているか確認
- plugin.yml の内容が正しいか確認

### エレベーターが動かない
- config.yml で `enabled: true` になっているか確認
- 鉄ブロックの上に2ブロック分の空間があるか確認
- デバッグモードを有効にしてログを確認

### ビルドエラーが発生する
- Java 17以上がインストールされているか確認
- インターネット接続があるか確認（依存関係のダウンロード用）
- `./gradlew clean build` を実行してキャッシュをクリア

## 期待される動作

✅ プレイヤーが鉄ブロックの上でジャンプすると上昇
✅ プレイヤーが鉄ブロックの上でスニークすると下降
✅ 移動先に十分な空間がある場合のみ移動
✅ 設定ファイルで速度をカスタマイズ可能
✅ コマンドでプラグイン情報を表示
✅ リロードコマンドで設定を再読み込み

## デバッグモード

config.yml で `debug: true` に設定すると、以下の情報がコンソールに出力されます：
- プレイヤーがエレベーターを使用した時のログ
- 上昇/下降の方向
- プレイヤー名

これにより、動作の詳細を追跡できます。
